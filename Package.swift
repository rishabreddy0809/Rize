// swift-tools-version:5.7
import PackageDescription

let package = Package(
    name: "Rize",
    dependencies: [
        // Add other dependencies here if needed
    ],
    targets: [
        .target(
            name: "Rize",
            dependencies: [
                // Add other dependencies here if needed
            ]
        ),
    ]
)
